name: Deploy to itch.io

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Fetch Latest Release and Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get Latest Tag
        id: get_release
        run: |
          latest_tag=$(git describe --tags `git rev-list --tags --max-count=1`)
          echo "release=$latest_tag" >> $GITHUB_OUTPUT
          echo "Found latest tag: $latest_tag"

      - name: Download Windows Installer
        uses: dsaltares/fetch-gh-release-asset@master
        with:
          repo: ${{ github.repository }}
          version: tags/${{ steps.get_release.outputs.release }}
          file: Only4BMS_Setup.exe
          target: dist/Only4BMS_Setup.exe

      - name: Download Mac Intel Zip
        uses: dsaltares/fetch-gh-release-asset@master
        with:
          repo: ${{ github.repository }}
          version: tags/${{ steps.get_release.outputs.release }}
          file: Only4BMS-Mac-Intel.zip
          target: dist/Only4BMS-Mac-Intel.zip

      - name: Download Mac ARM Zip
        uses: dsaltares/fetch-gh-release-asset@master
        with:
          repo: ${{ github.repository }}
          version: tags/${{ steps.get_release.outputs.release }}
          file: Only4BMS-Mac-ARM.zip
          target: dist/Only4BMS-Mac-ARM.zip

      - name: Install Butler
        run: |
          echo "Checking DNS for broth.itch.zone..."
          nslookup broth.itch.zone || echo "DNS lookup failed, but proceeding to try curl..."

          # Retry loop for download (up to 5 times)
          for i in {1..5}; do
            echo "Attempt $i: Downloading Butler from broth.itch.zone..."
            if curl -fL -o butler.zip https://broth.itch.zone/butler/linux-amd64/LATEST/archive/default; then
              echo "Download successful!"
              break
            else
              if [ $i -eq 5 ]; then
                echo "Failed to download Butler after 5 attempts."
                exit 1
              fi
              echo "Download failed. Retrying in 5 seconds..."
              sleep 5
            fi
          done

          unzip butler.zip
          chmod +x butler
          ./butler -V
          # Add to PATH so we can use 'butler' directly
          echo "$(pwd)" >> $GITHUB_PATH

      - name: Deploy Windows to itch.io
        env:
          BUTLER_API_KEY: ${{ secrets.ITCH_IO_API_KEY }}
        run: |
          butler push dist/Only4BMS_Setup.exe minwook-shin/only4bms:windows --userversion ${{ steps.get_release.outputs.release }}

      - name: Deploy Mac Intel to itch.io
        env:
          BUTLER_API_KEY: ${{ secrets.ITCH_IO_API_KEY }}
        run: |
          butler push dist/Only4BMS-Mac-Intel.zip minwook-shin/only4bms:macos-intel --userversion ${{ steps.get_release.outputs.release }}

      - name: Deploy Mac ARM to itch.io
        env:
          BUTLER_API_KEY: ${{ secrets.ITCH_IO_API_KEY }}
        run: |
          butler push dist/Only4BMS-Mac-ARM.zip minwook-shin/only4bms:macos-arm --userversion ${{ steps.get_release.outputs.release }}
